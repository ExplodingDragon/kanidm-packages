[
  {
    "name": "${NAME}-v${VERSION}-${BETA}.tar.gz",
    "urls": [
      "https://github.com/${NAME}/${NAME}/archive/refs/tags/v${VERSION}-${BETA}.tar.gz"
    ],
    "sha256sum": {
      "${NAME}-v${VERSION}-${BETA}.tar.gz": "SKIP"
    }
  }
]
